
/** \brief Calcula el factiorial retornando su resultado
 *
 * \param recibe como  parametro el numero a calcular
 * \param inicalizo un parametro para que asigne su resultado y luego lo retorne
 * \return retorna el resultado del A!
 *
 */
long int CalcularFactorial(float);
long int CalcularFactorial2(float);
/** \brief Calcula la division retornando su resultado
 *
 * \param recibe como  parametro el dividendo a calcular
 * \param recibe como  parametro el divisor a calcular
 * \param inicalizo un parametro para que asigne su resultado y luego lo retorne
 * \return retorna el resultado de la division
 *
 */
float Divission(float,float);
/** \brief Calcula la suma retornando su resultado
 *
 * \param recibe como  parametro 1 numero a calcular
 * \param recibe como  parametro 1 numero a calcular
 * \param inicalizo un parametro para que asigne su resultado y luego lo retorne
 * \return retorna el resultado de la suma
 *
 */


float SumarTotales(float,float);
/** \brief Calcula la resta retornando su resultado
 *
 * \param recibe como  parametro 1 numero a calcular
 * \param recibe como  parametro 1 numero a calcular
 * \param inicalizo un parametro para que asigne su resultado y luego lo retorne
 * \return retorna el resultado de la resta
 *
 */

float RestarTotales(float,float);
/** \brief Calcula la multiplicacion retornando su resultado
 *
 * \param recibe como  parametro 1 numero a calcular
* \param recibe como  parametro 1 numero a calcular
 * \param inicalizo un parametro para que asigne su resultado y luego lo retorne
 * \return retorna el resultado multiplicacion
 *
 */
float MultiplicarTotales(float,float);
/** \brief Carga la variable (tanto de opcion y el ingreso de los numeros a calcular)
 *
 * \param recibe como  parametro 1 numero a calcular
 * \param inicalizo un parametro para que asigne su resultado y luego lo retorne
 * \return retorna el resultado del numero cargado
 *
 */
float CargarNumeros(float);
/** \brief Muestra por pantalla el combo del menu
 *
 * \param recibe como  parametro 1 numero de la opcion que a elegido
 * \param inicalizo un parametro para que asigne su resultado y luego lo retorne
 * \return retorna el resultado es mostrar el menu por pantalla
 *
 */

void MostrarMenu(float,float );

